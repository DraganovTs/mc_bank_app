server:
  port: 8080
spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: tz
    password: ''
  h2:
    console:
      enabled: true
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true

build:
  version: "1.0"

accounts:
  message: "Welcome to bankcorp accounts related local APIs"
  contactDetails:
    name: "Gospodin Gospodinov"
    email: "gospodin@example.com"
  onCallSupport:
    -(888)888-8888
    -(999)999-9999